import { Placeholder } from "@/components/staff/Placeholder";

export default function CalendarTab() {
  return (
    <Placeholder
      title="Calendar"
      subtitle="Bookings and doctor availability at a glance. Coming in the next build."
    />
  );
}
