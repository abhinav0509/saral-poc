# Metro (React Native / Expo) cannot follow pnpm's symlinked node_modules,
# so we hoist into a flat top-level node_modules. Required once apps/staff lands.
node-linker=hoisted

# Keep peer-dependency resolution lenient across the RN/Expo + Next toolchains.
strict-peer-dependencies=false
